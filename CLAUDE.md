# Claude / Codex / Cursor — stop

You are in the McCluster ecosystem. This repo is a satellite of `mcclusterishere/mccluster`.

Read `AGENTS.md` first.
Worker: `mccluster` on `https://api.mccluster.org`.
There is no Worker named `mccluster-core`. Do not create one.
Do not grow a second backend here.
